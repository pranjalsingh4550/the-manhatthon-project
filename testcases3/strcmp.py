def main():
	l:str = "lenovo"
	l2:str = "dell"

	if l2 <= "DELL":
		return
	elif l2 == l:
		return
	elif l == "DELL":
		return
	if l == l2:
		print ("equal")
	if l < l2:
		print ("less than")
	if l <= l2:
		print ("less than/equal to")
	if l > l2:
		print ("greater than")
	if l >= l2:
		print ("greater than/equal to")
	if l != l2:
		print ("not equal")

